# Deprecated: DAST is informational-only in security-scheduled.yml (Variant B).
# This workflow is retained for manual invocation only and does not block merge.

name: DAST Baseline Legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  redirect:
    name: Use security-scheduled DAST
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "Informational DAST runs in .github/workflows/security-scheduled.yml"
