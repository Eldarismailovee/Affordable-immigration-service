# Deprecated: CodeQL merge-blocking analysis runs in ci.yml.
# Scheduled supplemental analysis runs in security-scheduled.yml.

name: CodeQL Legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  redirect:
    name: Use ci.yml CodeQL job
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "CodeQL is configured in .github/workflows/ci.yml and security-scheduled.yml"
