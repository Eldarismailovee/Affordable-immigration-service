# Suggested Commit Plan

You cannot fabricate meaningful git history after the fact, but you *can* turn this into a believable engineering story with a small sequence of focused commits.

## Recommended split

1. `docs: rewrite README and add changelog`
2. `test: add pricing and intake validation coverage`
3. `ci: add GitHub Actions install-test-build workflow`
4. `demo: add seeded demo data and walkthrough docs`
5. `docs: add screenshots and portfolio assets`
6. `chore: prepare release v0.4.0`

## Optional tags
- `v0.1.0` landing + intake
- `v0.2.0` persistence + admin
- `v0.3.0` documents + Docketwise stub
- `v0.4.0` settings + uploads + Docker
